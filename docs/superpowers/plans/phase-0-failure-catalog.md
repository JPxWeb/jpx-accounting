# Phase 0 — Failure Catalog (2026-07-03)

Baseline runs on `feat/advisory-pivot` @ `1734bd7` (master plan Task 0.1).

## `pnpm check`

**PASS** (after a Prettier pass on the two new pivot docs). One pre-existing, non-fatal warning:

- `apps/web/components/capture/evidence-archive-table.tsx:65` — `react-hooks/incompatible-library` warning: React Compiler skips memoizing `useReactTable()`. Known TanStack Table v8 limitation, not actionable here.

## `pnpm test:e2e` — 11 failed / 7 skipped / 16 passed (4.4m)

| Spec                                             | Projects | Failure                                                                                                                                         | Root cause                                                                                                                                                                                                                                                                    | Resolution                                                                                                                                   |
| ------------------------------------------------ | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| `capture.spec.ts:34` axe serious/critical        | both     | `color-contrast` (serious), 3.31:1 dark text `#0b1720` on teal `#0f766e` (skip-link, active nav chips) + 3.99:1 `text-white/78` summary on teal | **App bug**: un-layered `a { color: inherit }` in `globals.css` beats Tailwind's layered `.text-white` utility (cascade layers: un-layered author CSS wins), so "white" anchor text silently rendered as body dark text on teal. Second: white/78 alpha insufficient on teal. | Fix in app: moved the `a` reset into `@layer base`; `text-white/78` → `text-white/90` (4.76:1, verified with culori). No axe rules disabled. |
| `home.spec.ts:16` add review item                | both     | `getByRole heading /Review queue/` absent; `queue-overflow-menu`/`simulate-upload` testids gone                                                 | **Stale test**: asserts the pre-D3 Today screen. "Review queue" is a `SectionLabel` now; simulate-upload was retired.                                                                                                                                                         | Test updated: asserts current h2, adds review via the real capture→promote journey (`/capture` quick-add → promote → review count 2).        |
| `home.spec.ts:35` approval + draft capture       | both     | `getByText("More capture options")` absent                                                                                                      | **Stale test**: capture sheet now shows modes directly (no disclosure step).                                                                                                                                                                                                  | Test updated: click `capture-mode-camera` directly.                                                                                          |
| `home.spec.ts:50` axe (contrast-excluded)        | both     | violations under the same helper                                                                                                                | Same `@layer` cascade bug surfaced other contrast-adjacent failures; re-verified after fix.                                                                                                                                                                                   | Covered by app fix above (re-run confirms).                                                                                                  |
| `navigation-and-share.spec.ts:9`                 | both     | `settings-hero` "About this build" not found on `/settings`                                                                                     | **Stale test**: PR-D2 moved the build/posture content to `/settings/about`; `/settings` redirects to `/settings/company`.                                                                                                                                                     | Test updated: expects redirect to `/settings/company`, then asserts posture ids on `/settings/about`.                                        |
| `mobile-bottom-clearance.spec.ts:16` `/settings` | mobile   | `billing-card` testid missing on `/settings`                                                                                                    | **Stale test**: `billing-card` lives on `/settings/about` since PR-D2.                                                                                                                                                                                                        | Test updated: path → `/settings/about`. Not a real dock-overlap regression.                                                                  |

## Follow-up findings (surfaced while fixing)

| Finding                                                              | Root cause                                                                                                                                                                                                                                                                                                                                | Resolution                                                                                                                                                                                                                                        |
| -------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `home.spec` axe: `aria-allowed-attr` (critical) on the status filter | Base UI `ToggleGroup` renders `aria-orientation` on `role="group"`, which ARIA does not permit.                                                                                                                                                                                                                                           | `components/ui/toggle-group.tsx` strips the attribute in a layout effect (orientation stays on `data-orientation` for styling).                                                                                                                   |
| Server state vanished across `page.goto` navigations in E2E          | **The E2E suite was never talking to the API.** `NEXT_PUBLIC_*` vars are inlined at build time, but `pnpm test:e2e` built without `NEXT_PUBLIC_API_BASE_URL`, so the browser client silently used the in-memory demo fallback store (reset on every full page load). `resetApiState` was resetting a server store the browser never read. | New `build:e2e` script (cross-env) inlines `NEXT_PUBLIC_ACCOUNTING_RUNTIME_MODE=demo`, `NEXT_PUBLIC_API_BASE_URL=/api-proxy`, `NEXT_PUBLIC_DISABLE_SW=true`; all E2E scripts build through it. E2E now exercises the real browser→proxy→API path. |

## Notes

- The "CI red on /capture axe" from the repo analysis under-reported: 11 E2E tests were red on main, most stale since the PR-D2/D3 redesigns (CI E2E is opt-in on PRs, so they never gated).
- The `@layer base` anchor fix is a **real user-facing bug fix**: active nav chips rendered dark-on-teal instead of white-on-teal in production builds.
- Environment: pnpm is provided via corepack shims at `%LOCALAPPDATA%\corepack-shims` (not on system PATH); husky hooks need it on PATH too.
