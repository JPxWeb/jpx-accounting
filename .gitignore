node_modules
.next
dist
.turbo
coverage
playwright-report
test-results
artifacts/
*.tsbuildinfo
pw-debug*.log
.env
.env.local
.env.*.local

# Screenshots / design artifacts
*.png
*.jpg
*.jpeg

# …but shipped assets and visual-regression baselines MUST be tracked
# (the blanket *.png above silently dropped the Phase 0 baselines).
!apps/web/public/**/*.png
!tests/e2e/*-snapshots/*.png

# …and the E2E capture fixtures (tiny real files driven via setInputFiles).
!tests/fixtures/*.jpg

# Playwright MCP
.playwright-mcp/

# Local Lighthouse JSON output (audit tooling)
lh-*.json

# Claude Code local settings (user-specific paths)
.claude/settings.local.json

# Git worktrees (isolated workspaces)
.claude/worktrees/
.worktrees/

# local deploy experiments
api-deploy-test/
