pnpm-lock.yaml
.next
out
build
api-deploy
playwright-report
node_modules
.cursor

# Local SDD/brainstorm scratch dir (untracked working notes) — excluded so
# `pnpm format:check` does not fail on in-flight planning markdown.
.superpowers/

# Per-developer harness state (Claude Code auto-edits permissions list);
# tracked so config is portable across machines but excluded from Prettier
# to avoid format-churn PRs when the harness adds session permissions.
.claude/settings.json
